{
  "name": "locus-web",
  "version": "0.3.0",
  "private": true,
  "description": "Locus landing page — static, dark, terminal aesthetic",
  "scripts": {
    "start": "npx --yes serve public -l 3000",
    "serve": "npx --yes serve public -l 3000",
    "dev": "npx --yes serve public -l 3000"
  },
  "engines": {
    "node": ">=18"
  }
}
