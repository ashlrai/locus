{
  "$schema": "https://json-schema.org/draft/2020-12/schema",
  "$id": "https://github.com/ashlrai/locus/schema/doctor.schema.json",
  "title": "Locus DoctorReport",
  "description": "Machine contract for `locus doctor --json`. Mission-control readiness pane. Never contains resolved secret values. Exit codes: SAFE=0, WARN=1, UNSAFE=2.",
  "type": "object",
  "additionalProperties": true,
  "required": [
    "version",
    "home",
    "seal_ok",
    "bindings",
    "runtime",
    "approvals",
    "pending_approvals",
    "dual_control_waiting",
    "phantom_on_path",
    "unresolved_phm",
    "autopin",
    "workspace",
    "audit",
    "findings",
    "issues",
    "verdict",
    "ok"
  ],
  "properties": {
    "version": {
      "type": "string",
      "description": "Locus crate/CLI version."
    },
    "home": {
      "type": "string",
      "description": "Effective LOCUS_HOME path."
    },
    "seal_ok": {
      "type": "boolean",
      "description": "Daemon seal key readable."
    },
    "bindings": {
      "type": "integer",
      "minimum": 0
    },
    "pinned": {
      "type": ["string", "null"],
      "description": "Active binding alias when pinned (backward-compatible)."
    },
    "pin": {
      "$ref": "#/$defs/doctorPin"
    },
    "pin_seal_ok": {
      "type": ["boolean", "null"]
    },
    "runtime": {
      "$ref": "#/$defs/runtimeDrift"
    },
    "approvals": {
      "$ref": "#/$defs/approvalsHealth"
    },
    "pending_approvals": {
      "type": "integer",
      "minimum": 0
    },
    "dual_control_waiting": {
      "type": "integer",
      "minimum": 0
    },
    "phantom_on_path": {
      "type": "boolean"
    },
    "unresolved_phm": {
      "type": "array",
      "items": { "type": "string" },
      "description": "Phantom secret names only — never values."
    },
    "autopin": {
      "$ref": "#/$defs/autopinStatus"
    },
    "workspace": {
      "$ref": "#/$defs/workspace"
    },
    "audit": {
      "$ref": "#/$defs/auditSummary"
    },
    "findings": {
      "type": "array",
      "items": { "$ref": "#/$defs/finding" }
    },
    "issues": {
      "type": "array",
      "items": { "type": "string" }
    },
    "verdict": {
      "type": "string",
      "enum": ["SAFE", "WARN", "UNSAFE"]
    },
    "ok": {
      "type": "boolean",
      "description": "True only when verdict is SAFE."
    }
  },
  "$defs": {
    "doctorPin": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "alias",
        "tenant",
        "binding_id",
        "expires_at",
        "seal_ok",
        "expired"
      ],
      "properties": {
        "alias": { "type": "string" },
        "tenant": { "type": "string" },
        "binding_id": { "type": "string" },
        "expires_at": { "type": "string" },
        "seal_ok": { "type": "boolean" },
        "principal": { "type": ["string", "null"] },
        "client": { "type": ["string", "null"] },
        "expired": { "type": "boolean" }
      }
    },
    "runtimeDrift": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "pinned",
        "seal_ok",
        "binding_present",
        "binding_id_match",
        "tenant_match",
        "providers_match",
        "frozen",
        "expired",
        "providers",
        "issues",
        "ok"
      ],
      "properties": {
        "pinned": { "type": "boolean" },
        "seal_ok": { "type": "boolean" },
        "binding_present": { "type": "boolean" },
        "binding_id_match": { "type": "boolean" },
        "tenant_match": { "type": "boolean" },
        "providers_match": { "type": "boolean" },
        "frozen": { "type": "boolean" },
        "expired": { "type": "boolean" },
        "session_id": { "type": ["string", "null"] },
        "binding_alias": { "type": ["string", "null"] },
        "binding_id_session": { "type": ["string", "null"] },
        "binding_id_file": { "type": ["string", "null"] },
        "tenant_session": { "type": ["string", "null"] },
        "tenant_file": { "type": ["string", "null"] },
        "providers": {
          "type": "array",
          "items": { "$ref": "#/$defs/providerView" }
        },
        "issues": {
          "type": "array",
          "items": { "type": "string" }
        },
        "ok": { "type": "boolean" }
      }
    },
    "providerView": {
      "type": "object",
      "additionalProperties": true,
      "required": ["provider", "account", "credential_ref", "orgs"],
      "properties": {
        "provider": { "type": "string" },
        "account": { "type": "string" },
        "credential_ref": {
          "type": "string",
          "description": "CredentialRef name only — never resolved secret."
        },
        "project_ref": { "type": ["string", "null"] },
        "team_id": { "type": ["string", "null"] },
        "account_id": { "type": ["string", "null"] },
        "read_only": { "type": ["boolean", "null"] },
        "orgs": {
          "type": "array",
          "items": { "type": "string" }
        }
      }
    },
    "approvalsHealth": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "dir",
        "exists",
        "writable",
        "total",
        "pending",
        "approved_valid",
        "expired_grants",
        "denied",
        "corrupt",
        "ok"
      ],
      "properties": {
        "dir": { "type": "string" },
        "exists": { "type": "boolean" },
        "writable": { "type": "boolean" },
        "total": { "type": "integer", "minimum": 0 },
        "pending": { "type": "integer", "minimum": 0 },
        "approved_valid": { "type": "integer", "minimum": 0 },
        "expired_grants": { "type": "integer", "minimum": 0 },
        "denied": { "type": "integer", "minimum": 0 },
        "corrupt": { "type": "integer", "minimum": 0 },
        "ok": { "type": "boolean" }
      }
    },
    "autopinStatus": {
      "type": "object",
      "additionalProperties": true,
      "required": [
        "path",
        "present",
        "remote_autopin_enabled",
        "remote_rules",
        "ok"
      ],
      "properties": {
        "path": { "type": "string" },
        "present": { "type": "boolean" },
        "auto_pin": { "type": ["string", "null"] },
        "remote_autopin_enabled": { "type": "boolean" },
        "remote_rules": { "type": "integer", "minimum": 0 },
        "ok": { "type": "boolean" },
        "note": { "type": ["string", "null"] }
      }
    },
    "workspace": {
      "type": "object",
      "additionalProperties": true,
      "required": ["found", "allowed_bindings", "require_pin"],
      "properties": {
        "found": { "type": "boolean" },
        "path": { "type": ["string", "null"] },
        "default_binding": { "type": ["string", "null"] },
        "allowed_bindings": {
          "type": "array",
          "items": { "type": "string" }
        },
        "require_pin": { "type": "boolean" },
        "pin_allowed": { "type": ["boolean", "null"] }
      }
    },
    "auditSummary": {
      "type": "object",
      "additionalProperties": true,
      "required": ["path", "total", "last", "scope_freeze", "deny"],
      "properties": {
        "path": { "type": "string" },
        "total": { "type": "integer", "minimum": 0 },
        "last": {
          "type": "array",
          "items": { "$ref": "#/$defs/auditEvent" }
        },
        "scope_freeze": { "type": "integer", "minimum": 0 },
        "deny": { "type": "integer", "minimum": 0 }
      }
    },
    "auditEvent": {
      "type": "object",
      "additionalProperties": true,
      "required": ["ts", "op", "binding"],
      "properties": {
        "ts": { "type": "string" },
        "op": { "type": "string" },
        "binding": { "type": "string" },
        "detail": {}
      }
    },
    "finding": {
      "type": "object",
      "additionalProperties": true,
      "required": ["severity", "code", "message"],
      "properties": {
        "severity": {
          "type": "string",
          "enum": ["warn", "unsafe"]
        },
        "code": { "type": "string" },
        "message": { "type": "string" }
      }
    }
  }
}
